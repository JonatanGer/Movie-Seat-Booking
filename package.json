{
  "name": "movie-seat-booking",
  "version": "1.0.0",
  "scripts": {
    "lint": "eslint src/**/*.js",
    "start": "json-server --watch data/movies.json --port 3000"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "json-server": "^0.17.4"
  }
}
